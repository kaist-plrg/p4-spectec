dec $identity(nat) : nat
  hint(prose_in %9)

dec $other(nat) : nat
  hint(prose_in %0)

var value : nat

def $identity(value) = value
def $other(value) = value

dec $use : nat
def $use = $identity(0)
