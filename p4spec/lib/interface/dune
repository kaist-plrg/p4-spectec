(library
 (name interface)
 (libraries core_unix menhirLib util domain xl il runtime_dynamic)
 (modules
  source
  context
  extract
  wrap
  flatten
  preprocessor
  lexer
  parse
  parser
  hint
  unparse
  parser_debug
  debug_config))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --table --inspection))
