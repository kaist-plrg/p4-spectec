(library
 (name interface)
 (libraries util domain xl il core_unix menhirLib)
 (modules source context extract wrap value preprocessor lexer parser parse))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --table --inspection))
