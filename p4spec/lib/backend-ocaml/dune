; compiled_stub/ is a committed template copied into compiled/ by
; `make restore-stub`; dune must not see it as a second `spec_parts` library.

(dirs :standard \ compiled_stub)

(library
 (name backend_ocaml)
 (public_name p4spectec.backend_ocaml)
 (libraries util domain frontend lang pass runtime spec_parts))
