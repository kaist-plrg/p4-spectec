var i : int

builtin dec $rev_<X>(X*) : X*
builtin dec $transpose_<X>(X**) : X**
builtin dec $assoc_<X, Y>(X, (X, Y)*) : Y?

dec $main() : int
def $main() = i
  -- if i_rev* = $rev_<int>([1, 2, 3])
  -- if i_rev1 = i_rev*[0]
  -- if i_tr** = $transpose_<int>([[1, 2, 3], [4, 5, 6]])
  -- if i_row* = i_tr**[0]
  -- if i_row1 = i_row*[0]
  -- if i_row2 = i_row*[1]
  -- if i_assoc = $assoc_<text, int>("y", [("x", 7), ("y", 8)])
  -- if i = $(i_rev1 + i_row1 + i_row2 + i_assoc + |i_tr**|)
