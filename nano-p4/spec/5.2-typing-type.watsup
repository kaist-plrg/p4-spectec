relation Type_ok:
  typingContext |- type ~> typeIR
  hint(input %0 %1)

rulegroup Type_ok {

  rule Type_ok/baseType:
    TC |- baseType ~> baseType

  rule Type_ok/typeName:
    TC |- (TID typeId) ~> dataTypeIR
    -- if dataTypeIR = $find_typeDef_t(TC, typeId)

}
