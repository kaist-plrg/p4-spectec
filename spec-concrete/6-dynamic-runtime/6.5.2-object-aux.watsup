;;
;; Object helpers
;;

;;
;;;; Object constructor checks
;;

dec $is_externObject(object) : bool
  hint(prose_true %0 "is an extern object")
  hint(prose_false %0 "is not an extern object")

def $is_externObject(externObject) = true
def $is_externObject(_) = false
  -- otherwise
