relation EntryIL:
  |- scriptIL : val
  hint(input %0)

rule EntryIL:
  |- scriptIL : val
  -- debug "entry-il"
  -- if CIL = $loadIL($emptyIL_ctx, scriptIL)
  -- debug "load complete"
  ---- ;; calls $main(), assuming it exists in the given script
  -- debug "into call"
  -- EvalIL_exp: CIL |- CALL "main" eps eps : OK val
  -- debug val
