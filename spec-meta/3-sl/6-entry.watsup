relation EntrySL:
  |- scriptSL : val
  hint(input %0)

rule EntrySL:
  |- scriptSL : val
  -- debug "entry-sl"
  -- if CSL = $loadSL($emptySL_ctx, scriptSL)
  -- debug "load complete"
  ---- ;; calls $main(), assuming it exists in the given script
  -- debug "into call"
  -- EvalSL_exp: CSL |- CALL "main" eps eps : OK val
  -- debug val
