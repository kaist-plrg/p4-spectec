relation Entry:
  |- script : val
  hint(input %0)

rule Entry:
  |- script : val
  -- if C = $load(script)
  -- debug "load complete"
  ---- ;; calls $main(), assuming it exists in the given script
  -- debug "into call"
  -- Eval_exp: C |- CALL "main" eps eps : OK val
  -- debug val
