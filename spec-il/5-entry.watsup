relation Entry:
  |- script : val
  hint(input %0)

rule Entry:
  |- script : val
  -- if C = $load(script)
  ---- ;; calls $main(), assuming it exists in the given script
  -- Eval_exp: C |- CALL "main" eps eps : OK val
  -- debug val
