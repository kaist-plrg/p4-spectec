[#sec-parser]
== Abstract parser machine

// 744
This section describes the P4 constructs specific to parsing network packets.

// 745
.Parser FSM structure.
[#fig-parserstatemachine]
image::parserstatemachine.png[width=150,align="center"]

// 746
A P4 parser describes a state machine with one start state and two final
states. The start state is always named `start`. The two final states are named
`accept` (indicating successful parsing) and `reject` (indicating a parsing failure).
The `start` state is part of the parser, while the `accept` and `reject` states
are distinct from the states provided by the programmer and are logically
outside of the parser. <<fig-parserstatemachine>> illustrates the general
structure of a parser state machine.

// Added
A parser declaration introduces a constructor for a parser object. See
<<sec-parser-declaration>> for details.

// Added
${syntax: parserDeclaration parserLocalDeclaration parserState}

include::01-local-declaration.adoc[]
include::02-state.adoc[]
include::03-statement.adoc[]
include::04-transition-statement.adoc[]
