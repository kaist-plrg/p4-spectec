[#sec-package-object]
=== Package objects

A package type declaration introduces a package object constructor.

${syntax: packageTypeDeclaration}

All parameters of a package are evaluated at compilation time, and in
consequence they must all be directionless (they cannot be `in`, `out`, or
`inout`). Otherwise package types are very similar to parser type declarations.
Packages can only be instantiated; there are no runtime behaviors associated
with them.

Its typing rule is explained in <<sec-sem-Decl_ok-packageTypeDeclaration>>.

${syntax: packageObjectConstructorDef}

Package objects are then instantiated by constructor invocations in
<<sec-callExpression-constructor>> and <<sec-instantiation>> to yield:

${syntax: packageObject}

===== Type nesting

${table: nestable_constructor_package}

[#sec-sem-ConstructorType_wf-packageObjectTypeIR]
==== Well-formedness

${rulegroup: ConstructorType_wf/packageObjectTypeIR}
${ruleprose: ConstructorType_wf/packageObjectTypeIR}
