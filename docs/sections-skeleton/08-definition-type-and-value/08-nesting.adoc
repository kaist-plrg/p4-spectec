=== Type Nesting Rules

===== Alias types
${table-prose: nestable_alias}

===== Data types
${table-prose: nestable_data}

===== Synthesized types
${table-prose: nestable_synthesized}

===== Callables
${table-prose: nestable_callable}

===== Constructor types
${table-prose: nestable_constructor}
